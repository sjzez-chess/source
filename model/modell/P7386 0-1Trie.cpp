#include <bits/stdc++.h>

using namespace std;

const int INF = 18888913;

int T;

inline int read()
{
	int x=0,f=1;char ch=getchar();
	while (ch<'0'||ch>'9'){if (ch=='-') f=-1;ch=getchar();}
	while (ch>='0'&&ch<='9'){x=x*10+ch-48;ch=getchar();}
	return x*f;
}

int main()
{
    T = read();
    
    while (T -- )
    {
        int n, m; // n 代表 1 的个数，m 代表 0 的个数
        n = read(), m = read();

        

    }
    return 0;
}